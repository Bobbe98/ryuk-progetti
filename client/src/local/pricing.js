// Mirrors server/src/seed/pricing.js (rank only — homebrew items get their
// gp cost directly from the form, not auto-derived from rarity).
export const RARITY_RANK = {
  common: 0, uncommon: 1, rare: 2, very_rare: 3, legendary: 4, artifact: 5, varies: 1.5,
};
